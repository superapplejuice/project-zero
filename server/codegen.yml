overwrite: true
schema: http://localhost:5000
documents: null
config:
  skipTypename: true
  contextType: ./context#Context
generates:
  src/resolvers/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
hooks:
  afterAllFileWrite:
    - prettier --write
