import React from 'react'

import { LoaderProps } from './types'

import * as Styles from './styles'

const Loader = ({ size }: LoaderProps) => <Styles.Container size={size} />

export default Loader
